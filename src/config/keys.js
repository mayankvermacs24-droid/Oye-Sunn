/**
 * API Keys Configuration
 * 
 * ⚠️  REPLACE the placeholder below with your actual Google Maps API key.
 * 
 * Required APIs enabled on this key (Google Cloud Console):
 *   1. Places API (New)         — for autocomplete search
 *   2. Maps SDK for Android     — for the MapView
 *   3. Maps SDK for iOS         — if testing on iOS
 *   4. Geocoding API            — for reverse geocoding
 */

export const GOOGLE_MAPS_API_KEY = 'YOUR_GOOGLE_API_KEY_HERE';
